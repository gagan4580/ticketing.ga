name: tests

on:
  pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v2
       - run: cd auth && docker build -t gagan2756450/auth .
       - run: docker push gagan27564580/auth
